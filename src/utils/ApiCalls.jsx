class Api {

  function calculatePrice(settings) {
    //Passe en param les parametres d'une commande et retourne le prix
  }

  function checkPromo(codePromo) {
    //Verifie si un code promo est valable
  }

  function sendCommand(command) {
    //Envoie d'une commande finalisée
  }

  function logIn(user) {
    //Tentative de login avec infos fournis par user
  }

}
export default Api
