import '../Actions/Actions';


function AppReducer(state = initialState, action) {
  //switch case avec chaque action et assignation des datas en fonction, return state en default
}

export default AppReducer;
